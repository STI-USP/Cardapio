# Metrics pipeline configuration (copy to metrics.env and adjust)

# Project paths
PROJECT_DIR="$(pwd)"
WORKSPACE="Cardapio USP.xcworkspace"
SCHEME="Cardapio USP"
OUTPUT_DIR="$PROJECT_DIR/metrics-reports"
SIMULATOR="platform=iOS Simulator,name=iPhone 14"

# SonarQube (optional)
SONAR_HOST_URL="http://localhost:9000"
SONAR_PROJECT_KEY="cardapio-usp-m1"
SONAR_LOGIN=""  # set token here or via environment

# Tooling
EXCLUDE_DIRS="Pods,Index,Build"
LIZARD_LANGUAGE="objectivec"

# Test options
ENABLE_CODE_COVERAGE="YES"
RESULT_BUNDLE_NAME="TestResults.xcresult"

# Branch tag for reports (used in filenames/metadata)
METRICS_TAG="local"
